syntax = "proto3";

import "models.proto";
//import "google/protobuf/empty.proto";

package nn_go;

option go_package = "service/nn_go";

service ClassifyNN {
  rpc CreateTask (TaskOneRequest) returns (TaskOneResponse);
  // rpc Login(RequestLogin) returns (ResponseJWT);
  // rpc Stats(google.protobuf.Empty) returns (ResponseStats);
}
